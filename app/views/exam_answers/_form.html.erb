<%= form_for([@curriculum, @exam_question, @exam_answer]) do |f| %>
  <% if @exam_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam_answer.errors.count, "error") %> prohibited this exam_answer from being saved:</h2>

      <ul>
      <% @exam_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  設問:<br />
  <%= @exam_question.question %><br /><br />
  <div class="field">
    <%= f.label :answer, "解答:" %><br />
    <%= f.text_area :answer %>
  </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
